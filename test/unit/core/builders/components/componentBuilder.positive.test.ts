// import { componentBuilder } from '@app/core/builders/components';

describe('core', () => {
  describe('builders', () => {
    describe('components', () => {
      describe('componentBuilder', () => {
        describe('positive', () => {
          test('should return the expected set of components', () => {
            // Arrange

            // Act

            // Assert
            expect(true).toEqual(true);
          });
        });
      });
    });
  });
});
